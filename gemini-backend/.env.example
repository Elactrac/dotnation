# ==============================================
# DotNation Gemini Backend - Environment Config
# ==============================================

# Server Configuration
NODE_ENV=development  # development | production
PORT=3001
LOG_LEVEL=debug  # error | warn | info | http | debug

# Redis Configuration
# Local development: redis://localhost:6379
# Production: Get from Redis Cloud (https://redis.com/try-free/)
REDIS_URL=redis://localhost:6379

# Google Gemini API
# Get your FREE API key from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Security & Authentication
# Generate secure keys with: openssl rand -hex 32
BACKEND_API_KEY=your_secure_backend_api_key_here

# CORS Configuration
# Comma-separated list of allowed origins
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000  # 15 minutes in milliseconds
RATE_LIMIT_MAX_REQUESTS=50
AI_RATE_LIMIT_MAX=10  # Max AI requests per window

# Error Tracking (Optional but Recommended)
# Get from: https://sentry.io/signup/
SENTRY_DSN=

# Session Configuration
SESSION_MAX_AGE=300000  # 5 minutes in milliseconds
SESSION_MAX_ATTEMPTS=3
SESSION_LOCKOUT_DURATION=60000  # 1 minute in milliseconds

# Cache Configuration
CACHE_TTL_SECONDS=3600  # 1 hour

# Request Configuration
REQUEST_TIMEOUT_MS=30000  # 30 seconds
AI_REQUEST_TIMEOUT_MS=45000  # 45 seconds

# Production-Only Settings
# ==============================================

# Metrics (Prometheus)
ENABLE_METRICS=false  # Set to true in production

# Health Check Configuration
HEALTH_CHECK_INTERVAL_MS=60000  # 1 minute

# Log Retention
LOG_MAX_FILES=30d  # Keep logs for 30 days
LOG_MAX_SIZE=20m  # Max size per log file

# ==============================================
# Notes:
# ==============================================
# 1. NEVER commit this file with real credentials
# 2. Generate secure keys: openssl rand -hex 32
# 3. Use different keys for dev/staging/production
# 4. Store production secrets in your hosting provider's secrets manager
# ==============================================
